export default function Login({onLogin}) {
    return (
        <>
            <h2>Home (Public)</h2>
        
            <a href="/auth/google/login">
                Sign In with Google
            </a>
        </>
    );
}